Aloha <%= @recipient.name %>,

Here is the current inventory for <%= Time.now.strftime('%D') %>:

<%= "Location".ljust(20) %><%= "Product".ljust(25) %><%= "Quantity".ljust(10) %><%= "Harvested on".ljust(9) %>
<%- @products.each do |product| %><%= product.location.slice(0..19).ljust(20) %><%= product.name.slice(0..24).ljust(25) %><%= product.quantity.to_s.ljust(10) %><%= product.harvested_on.strftime('%D').ljust(9) %>
<%- end %>
Please remember products and their quantity may change during the course of the day. 
Check out http://surplus.localgarden.us for the most up to date information.

Enjoy!
